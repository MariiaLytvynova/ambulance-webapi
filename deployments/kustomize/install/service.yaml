kind: Service
apiVersion: v1
metadata:
  name: ml-ambulance-webapi
spec:
  selector:
    pod: ml-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port